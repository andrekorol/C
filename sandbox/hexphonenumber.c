#include <stdio.h>

int main() {
	unsigned long long phone_number = 5512988336480;
	printf("phone number = %llu\n", phone_number);
	printf("phone number in hex(uppercase) = %llX\n", phone_number);
	printf("phone number in hex(lowercase) = %llx\n", phone_number);

	return 0;
}
